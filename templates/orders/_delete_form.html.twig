<form method="post" action="{{ path('orders_delete', {'idOrder': order.idOrder}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order.idOrder) }}">
    <button class="btn">Delete</button>
</form>
